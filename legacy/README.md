# Código legado

## `legacy/client/` — proxy TCP local

Implementação anterior do requisito “cliente com thread de `recv`” + ponte HTTP para o navegador.

**Não é usado** no deploy Fly.io atual (100% web). Mantido para referência no relatório e testes locais opcionais.

Ver `legacy/client/README.md`.
