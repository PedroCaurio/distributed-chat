# Dev: proxy Vite → cliente HTTP (:8080)
VITE_API_URL=/api

# Console do navegador (F12) — logs da camada web
VITE_DEMO_LOGS=true

# Produção (build no Docker): mesma origem, deixe vazio ou omita
# VITE_API_URL=
