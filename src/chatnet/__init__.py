"""
chatnet — Chat multiusuário com sockets TCP, threads e interface web (SSE).

Pacote principal do projeto. Entrada: ``python -m chatnet`` ou ``python stack.py`` na raiz.
"""

__version__ = "2.0.0"
