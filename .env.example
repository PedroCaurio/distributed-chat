# Copie para .env (teste local com stack.py)

REDIS_URL=rediss://default:SENHA@HOST.upstash.io:6379

# HTTP do proxy (navegador)
PORT=8080

# TCP do servidor de chat (mesmo PC, stack.py)
SERVER_HOST=127.0.0.1
SERVER_PORT=9000
