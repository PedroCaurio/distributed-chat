# Deploy em produção

Use o guia gratuito: **[DEPLOY_FREE.md](./DEPLOY_FREE.md)**.

- **Recomendado (2 instâncias, $0):** Fly.io + Upstash  
- **Alternativa (1 instância, $0):** Render Free + Upstash  

Não use `plan: starter` nem `numInstances: 2` no Render se o objetivo é **US$ 0**.
